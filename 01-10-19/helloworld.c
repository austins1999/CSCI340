#include <stdio.h>

int main(int argc, char** argv) //int argc is the length
				//char** argv is an array and could
				//also be expressed as char*[] argv
{
	printf("Hello World\n");
	return 0; //Must return 0 since main() has return type int
}
